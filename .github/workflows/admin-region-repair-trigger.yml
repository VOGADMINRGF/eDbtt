name: Admin Region Repair Trigger

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/admin-region-repair-trigger.yml

permissions:
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger admin region queue repair"
