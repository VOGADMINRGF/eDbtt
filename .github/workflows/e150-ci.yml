name: E150 CI (Legacy)

# Legacy workflow for frühere E150-Pipeline. Läuft aktuell nicht automatisch;
# Vercel übernimmt Deploy/Build. Bei Bedarf manuell starten.
on:
  workflow_dispatch:

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      mongo:
        image: mongo:6
        ports: [ "27017:27017" ]
        options: >-
          --health-cmd="mongosh --eval 'db.runCommand({ ping: 1 })'" --health-interval=10s
      redis:
        image: redis:7-alpine
        ports: [ "6379:6379" ]
      neo4j:
        image: neo4j:5.21
        ports: [ "7474:7474", "7687:7687" ]
        env:
          NEO4J_AUTH: neo4j/test1234
          NEO4J_PLUGINS: '["apoc","graph-data-science"]'
          NEO4J_dbms_security_procedures_unrestricted: "apoc.* , gds.*"
          NEO4J_dbms_security_procedures_allowlist: "apoc.* , gds.*"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml

      - name: Env
        run: |
          cp .env.example .env || true
          echo "CORE_MONGODB_URI=mongodb://localhost:27017" >> .env
          echo "CORE_DB_NAME=edebatte_core" >> .env
          echo "VOTES_MONGODB_URI=mongodb://localhost:27017" >> .env
          echo "VOTES_DB_NAME=edebatte_votes" >> .env
          echo "PII_MONGODB_URI=mongodb://localhost:27017" >> .env
          echo "PII_DB_NAME=edebatte_pii" >> .env
          echo "MONGODB_URI=mongodb://localhost:27017" >> .env
          echo "MONGODB_DB=edebatte_core" >> .env
          echo "REDIS_URL=redis://localhost:6379" >> .env
          echo "JWT_SECRET=$(openssl rand -hex 16)" >> .env
          echo "NEO4J_URL=bolt://localhost:7687" >> .env
          echo "NEO4J_USER=neo4j" >> .env
          echo "NEO4J_PASSWORD=test1234" >> .env

      - name: pnpm install (root)
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint

      - name: Unit tests (coverage≥85%)
        run: pnpm run --if-present test:ci

      - name: Build web
        run: pnpm --filter @vog/web run build

      - name: SAST (semgrep)
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/ci

      - name: Secrets scan (gitleaks)
        uses: zricethezav/gitleaks-action@v2

      - name: Docker build (non-root)
        run: |
          docker build -f apps/web/Dockerfile -t vog-web:ci .
          docker run --rm --entrypoint id vog-web:ci -u

      - name: Container scan (trivy)
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: vog-web:ci
          severity: CRITICAL,HIGH
          exit-code: '1'
          vuln-type: 'os,library'

  zap-baseline:
    runs-on: ubuntu-latest
    needs: build-test-scan
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      # Optional: eigenes .env für den Web-Container, falls compose das liest
      - name: Prepare .env.ci
        run: |
          cp .env.example .env.ci || true
          echo "NODE_ENV=production" >> .env.ci
          echo "PORT=3000" >> .env.ci
          echo "CORE_MONGODB_URI=mongodb://localhost:27017" >> .env.ci
          echo "CORE_DB_NAME=edebatte_core" >> .env.ci
          echo "VOTES_MONGODB_URI=mongodb://localhost:27017" >> .env.ci
          echo "VOTES_DB_NAME=edebatte_votes" >> .env.ci
          echo "PII_MONGODB_URI=mongodb://localhost:27017" >> .env.ci
          echo "PII_DB_NAME=edebatte_pii" >> .env.ci
          echo "MONGODB_URI=mongodb://localhost:27017" >> .env.ci
          echo "MONGODB_DB=edebatte_core" >> .env.ci
          echo "REDIS_URL=redis://localhost:6379" >> .env.ci
          echo "JWT_SECRET=$(openssl rand -hex 16)" >> .env.ci
          echo "NEO4J_URL=bolt://localhost:7687" >> .env.ci
          echo "NEO4J_USER=neo4j" >> .env.ci
          echo "NEO4J_PASSWORD=test1234" >> .env.ci

      # App + Dependencies hochfahren (services: web, mongo, redis, neo4j in compose/prod.yml)
      - name: Start app via docker compose
        run: |
          docker compose -f compose/prod.yml --env-file ./.env.ci up -d web mongo redis neo4j
          echo "Waiting for web to be healthy..."
          for i in {1..40}; do
            if curl -fsS http://localhost:3000/api/health || curl -fsS http://localhost:3000/ > /dev/null; then
              echo "Web is up"; break
            fi
            sleep 3
          done
          curl -fsS http://localhost:3000/ >/dev/null || (echo "Web not reachable" && docker compose -f compose/prod.yml logs web && exit 1)

      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://localhost:3000'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a -d -m 5'
          # Artefakte
          allow_issue_writing: false
          # HTML Report wird als artifact ausgegeben
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ZAP HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-baseline-report
          path: |
            report_html.html
            owasp-zap*/*.html
            owasp-zap*/*.log
          if-no-files-found: warn

      - name: Docker Compose logs (on failure)
        if: failure()
        run: |
          docker compose -f compose/prod.yml ps
          docker compose -f compose/prod.yml logs --no-color web mongo redis neo4j | tail -n 2000
