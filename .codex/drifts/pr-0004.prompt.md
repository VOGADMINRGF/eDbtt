Hard Guardrails (MUSS als erstes laufen):
1) Gib aus: pwd, git rev-parse --show-toplevel, git remote -v.
2) Wenn:
   - pwd oder git rev-parse NICHT "edebatte-org" enthaelt, ODER
   - git remote -v NICHT VOGADMINRGF/edebatte-org zeigt, ODER
   - Pfad Archiv/ enthaelt
   => STOP. Keine Aenderungen. Nur melden: "Falsches Repo/Verzeichnis".

PR-0004: Block D Persist/Types (Eventualities)

Ziel
- Eventualities/DecisionTree Typen und Persistenz definieren.

Aufgaben (max 6)
1. Eventuality/DecisionTree Types und Prisma-Modelle definieren.
2. Migrationen + minimale API-Routen anlegen.
3. Service-Layer mit Validierung ergaenzen.
4. Read-only Admin-Liste fuer Eventualities.

Verification (Pflicht)
- `./verify.sh`

Doku (Pflicht)
- `docs/E150/Part15.md` aktualisieren (Ziel, Changes, Verification, Next Steps).

Output (Pflicht)
- Changes
- Verification (Commands + Pass/Fail)
- Next Steps
