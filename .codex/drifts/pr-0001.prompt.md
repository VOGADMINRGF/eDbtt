Hard Guardrails (MUSS als erstes laufen):
1) Gib aus: pwd, git rev-parse --show-toplevel, git remote -v.
2) Wenn:
   - pwd oder git rev-parse NICHT "edebatte-org" enthaelt, ODER
   - git remote -v NICHT VOGADMINRGF/edebatte-org zeigt, ODER
   - Pfad Archiv/ enthaelt
   => STOP. Keine Aenderungen. Nur melden: "Falsches Repo/Verzeichnis".

PR-0001: Part02 baseline (XP/Levels/Gamification)

Ziel
- Baseline fuer XP/Levels/Gamification gemaess Part02 herstellen (minimal, ohne Scope-Creep).

Aufgaben (max 6)
1. Scope gegen `docs/E150/Part02_Roles_Levels_XP_Gamification.md` abgleichen und Ziel-Dateien festlegen.
2. XP/Level-Konstanten und Shared Types definieren/aktualisieren.
3. Baseline XP-Akkumulations-Hooks implementieren (minimaler Pfad).
4. Level-Thresholds + Labels-Mapping hinterlegen.

Verification (Pflicht)
- `./verify.sh`

Doku (Pflicht)
- `docs/E150/Part15.md` aktualisieren (Ziel, Changes, Verification, Next Steps).

Output (Pflicht)
- Changes
- Verification (Commands + Pass/Fail)
- Next Steps
