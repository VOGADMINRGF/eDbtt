# Scope boundary

Dieser Draft-PR implementiert den providerneutralen Audio-/Caption-Vertrag, reale lokale Audioinspektion und Normalisierung, revisionsgebundene WebVTT/SRT-Artefakte, Safe Areas und fokussierte Contract-Tests. Er enthält kein Lip-Sync, keinen externen Upload, kein Scheduling und kein Publishing.
